# Academic Service - Variáveis de Ambiente

# Servidor
PORT=3002
NODE_ENV=development

# Banco de Dados PostgreSQL
DB_HOST=academic-db
DB_PORT=5432
DB_NAME=academic_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT (mesma chave do auth-service)
JWT_SECRET=sua_chave_secreta_aqui_mude_em_producao
